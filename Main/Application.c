#include <stdio.h>

typedef enum{
    FALSE, TRUE
}booleano;

typedef struct{
    int numeroLinhas;
    int numeroColunas;
} Matriz;


int main() {
    char equacao[40];
    while (TRUE){
        scanf("%[^\n]s", )
    }
    
    
}